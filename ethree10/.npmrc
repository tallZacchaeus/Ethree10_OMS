auto-install-peers=true
enable-pre-post-scripts=true
node-linker=node-modules
ignore-scripts=false
# Don't run an auto `pnpm install` (which errors on unapproved build scripts)
# before every `pnpm run` — scripts like dev:next should start directly.
verify-deps-before-run=false
